language: python
cache: pip

dist: xenial
python: 
  - 3.6
  - 3.7
install: pip install -r requirements.txt

matrix:
  include:
    - name: "Python 3.7 Lint"
      script:
      - flake8
      - mypy
